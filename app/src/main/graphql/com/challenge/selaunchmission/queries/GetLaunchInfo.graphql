query GetLaunchInfo($launchId: ID!) {
    launch(id: $launchId) {
        id
        site
        mission {
          name
          missionPatch
        }
        rocket {
          id
          name
          type
        }
        isBooked
      }
}
